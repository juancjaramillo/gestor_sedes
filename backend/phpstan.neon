includes:
  # (sin larastan)

parameters:
  level: 8
  paths:
    - app
    - database
    - tests

  ignoreErrors:
    - message: '#@mixin contains unknown class App\\Models\\IdeHelper.*#'
      paths:
        - app/Models/*
    - message: '#uses generic trait\\s+Illuminate\\\\Database\\\\Eloquent\\\\Factories\\\\HasFactory but does not specify its types#'
      paths:
        - app/Models/*
